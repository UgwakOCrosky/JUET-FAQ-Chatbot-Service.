1) open anaconda prompt
2) create new env, type:

   >>conda create -n chatbot python=3.5 anaconda

3) >>activate chatbot
4) >>pip install tensorflow==1.0.0
5) open spyder, type
   >> spyder
6) in spyder select the code till line number: 1 to 435 and run
7) after successfull training, check for checkpoint.ckpt in the PWD (just to be sure that checkpoint file is created).
8) close spyder and stop kernel, zip this entire folder and upload this zip on any drive.


Thank you in advance :)
